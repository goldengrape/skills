interface:
  display_name: "Delegate to ChatGPT Web"
  short_description: "Safely exchange ZIP tasks with ChatGPT Web."
  default_prompt: "Use $delegate-to-chatgpt-web to delegate this bounded task through the signed-in Codex in-app Browser, upload only explicitly authorized inputs as a ZIP, and download, validate, and safely extract only the matching result ZIP."
